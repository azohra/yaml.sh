defaults: &defaults
  region: us-west-2
  replicas: 2
  resources:
    cpu: 250m
    memory: 256Mi

shared: *defaults

staging:
  <<: *defaults
  replicas: 1
  domain: staging.example.com

production:
  <<: *defaults
  replicas: 6
  domain: example.com
